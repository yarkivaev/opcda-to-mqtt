# Production dependencies
# Install in this order on Python 3.4:

# 1. lxml (must install old version first, new versions need Python 3.5+)
lxml==4.3.5

# 2. OPC-UA server library
opcua==0.90.4

# 3. Windows-only dependencies (install manually on Windows)
# pip install pywin32
# pip install OpenOPC-Python3x
